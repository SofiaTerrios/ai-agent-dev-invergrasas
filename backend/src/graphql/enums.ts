export enum Role {
  admin = 'admin',
  empleado = 'empleado',
}

export enum Producto {
  RBD = 'RBD',
  Oleina = 'Oleina',
}

export enum TipoEmpaque {
  Granel = 'Granel',
  Caneca = 'Caneca',
  Balde = 'Balde',
}

export enum CertificadoTipo {
  oleina = 'oleina',
  rbd = 'rbd',
}
